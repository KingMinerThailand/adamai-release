# AdamAI Release

Public installer and release artifacts for AdamAI local beta.

Install:

```bash
curl -fsSL "https://github.com/KingMinerThailand/adamai-release/releases/latest/download/install.sh" | bash
```
